{% extends 'base.html.twig' %}

{% block title %}Ajouter un(des) module(s) à {{ categorie.intitule }}{% endblock %}

{% block body %}

<div id="wrapper-main">
    <h2>Choisir le(s) module(s) à ajouter</h2>
    {{ form_start(form) }}
    {{ form_widget(form.module) }}
    <br />
    {{ form_row(form.ajouter) }}
    {{ form_end(form) }}
</div>

{% endblock %}